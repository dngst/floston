<% if current_user&.id == article.user_id || (current_user&.admin_id == article.user_id && article.published) %>
  <p class="text-xl mt-2 underline mb-4">
    <%= link_to article do %>
      <%= article.title %>
    <% end %>
  </p>
<% end %>
