<div class="border border-gray-300 p-4 rounded mt-4">
  <%= link_to user_path(user), draggable:false, data: { turbo_frame: "_top" } do %>
    <p class="text-xl font-semibold text-flostonlink hover:underline capitalize"><%= user.name %></p>
  <% end %>
  <p class="mt-2 text-gray-800"><%= user.phone_number %></p>
</div>
