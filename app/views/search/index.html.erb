<%= turbo_frame_tag :results_list do %>
  <div class="max-w-lg">
    <p class="text-4xl mb-8 text-gray-800">
      Search results <%= params[:q].present? ? "for #{params[:q]}" : "" %>
    </p>

    <%= render "search_tenants" %>

    <%= render "search_properties" %>

    <%= render "search_requests" %>

    <%= render "search_articles" %>
  </div>
<% end %>
