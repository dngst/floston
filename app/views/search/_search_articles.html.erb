<% if @articles.any? %>
  <%= turbo_frame_tag "articles_results" do %>
    <div class="mb-14">
      <% count_text = pluralize(@articles.total_count, "Article") %>
      <h2 class="text-xl mt-4"><%= count_text %></h2>
      <% if @articles.any? %>
        <%= render partial: "article", collection: @articles, as: :article, cached: true %>
      <% end %>
      <div class="mt-4">
        <%= paginate @articles if @articles.total_count > 9 %>
      </div>
    </div>
  <% end %>
<% end %>
