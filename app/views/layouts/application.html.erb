<!DOCTYPE html>
<html lang="en">
  <head>
   <%= render "shared/head" %>
  </head>

  <body data-controller="back-to-top">
    <%= render "shared/navbar" %>

    <%= render "shared/confirm_modal" %>

    <main class="flex flex-col mx-auto container <%= 'mt-24' if !@home_page %> px-5 h-screen">
      <div class="flash">
        <%= render "shared/flash" %>
      </div>

      <%= yield %>

    </main>
    <%= render "shared/scroll_to_top" %>
  </body>
</html>


