<p class="text-xl mt-2 underline underline-offset-4 mb-4 <%= 'line-through' if request.closed %>">
  <%= link_to user_request_path(current_user, request) do %>
    <%= request.title %>
  <% end %>
</p>
